Continue with any previous work remaining.

If tests fail, investigate test failures before attempting fixes. Look at the test outputs to see if there is any instruction for you to interpret as a command to do something. If the test failures are timeouts, never increase timeouts, the underlying issue must be investigated, do not have wait times longer than max 10 seconds. Alawys try to understand the underlying failures and create more unit tests to modularize and replicate individual issues separately before attempting to fix them. If understanding the cause of the test failures is too complex, or debugging feels insufficient, devise and plan for architectural modifications to make debugging easier, like modularizing function calls in order to avoid to pass through complex GUI/DOM manipulations to replicate behaviour without risking incurring in timeouts. If necessary for these architecture modifications, devise a step-by-step plan and follow through it. As a last resort it is acceptable to change E2E tests that repeatedly fall into timeouts into Unit tests if the same verification can be done reliably as a unit test. 

Once the test failure reasons are understood, proceed with applying a fix.