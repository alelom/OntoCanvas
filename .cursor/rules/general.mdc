---
alwaysApply: true
---

This project uses `uv` as a package manager.

Whenever a modification to the Visualizer or Editor is requested, either take a screenshot or connect to the browser MCP to verify the output.


When you add or modify features, do it in a tidy and modular way with multiple files, as not to overwhelm the `main.ts` file which is already very long in terms of number of lines of code.

You must ensure to add relevant Integration/E2E test every time you fix or add a feature. 

You must run tests every time you finish fixing or adding some feature and ensure they pass.

The users of this static web app, which is deployed on github pages, will never have the option to deploy any additional service (e.g. Proxy) on their own to make this app work. Do not propose solutions that involve such mechanisms.

When adding features or implementing fixes, also make sure to implement required Unit tests, E2E tests, and make the solution as modular as possible, minimising the amount of code in the main.ts file and even moving out any relevant functionality to put it into separate files, as the main.ts is alreay too long.

Every time you need to write some E2E-test-specific functionality next to the implementation, do not add that in the same file as the implementation; create a separate E2E-functionality dedicated file for that file containing the required E2E functionality. If you find E2E functionality in implementation files, move it out as needed.

In tests, make timeouts reasonably short, generally not over 10 seconds, to avoid tests taking too long to fail. Whenever a E2E test fails for timeouts over 10 seconds, it's almost never due to a timeout issue that can be resolved by increasing timeouts, it's most likely a deeper issue that needs investigation, or an issue that can be resolved with a full page refresh between tests. Do not increase timeouts indiscriminately unless we are almost sure it's a timeout issue. If possible, limit maximum timeout everywhere to at most 10 seconds, and specifically where the dev server may need starting up; everywhere else, ensure timeouts do not exceed 5 seconds. In E2E tests, you need a full page refresh between tests rather than increasing the timeout to 10s with await waitForGraphRender(page, 10000); it's likely that any wait time over 5 seconds is just wasted time.

When running tests with timeout, e.g. npm run test:e2e -- tests/e2e/annotationPropertyWarning.e2e.test.ts --reporter=verbose --testTimeout=5000, make sure to not exceeed a timeout value over 5-10s to avoid wasting time as mentioned above. I.e. the max timeout should be set with --testTimeout=5000. If the test throws timeout again, the real issue won't be resolved by exceeding the value over 5 seconds, it's some deeper issue.

Always Use `debugLog` Instead of `console.log` and similar for warning/errors for debugging purposes. See [src/utils/debug.ts](mdc:src/utils/debug.ts) for implementation details.